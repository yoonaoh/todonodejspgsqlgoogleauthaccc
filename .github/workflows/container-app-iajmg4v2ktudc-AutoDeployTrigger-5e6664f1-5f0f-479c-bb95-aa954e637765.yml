name: Trigger auto deployment for container-app-iajmg4v2ktudc

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - 'server/**'
    - '.github/workflows/container-app-iajmg4v2ktudc-AutoDeployTrigger-5e6664f1-5f0f-479c-bb95-aa954e637765.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPIAJMG4V2KTUDC_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPIAJMG4V2KTUDC_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPIAJMG4V2KTUDC_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}/server 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPIAJMG4V2KTUDC_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPIAJMG4V2KTUDC_REGISTRY_PASSWORD }}
          containerAppName: container-app-iajmg4v2ktudc
          resourceGroup: todonodejspgsqlgoogleauthacccb489
          imageToBuild: default/container-app-iajmg4v2ktudc:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


